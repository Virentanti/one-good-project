def compare(a,b):
    a.sort()
    b.sort()
    for i in range(len(a)):
        if a[i].upper()!=b[i].upper():
            return False
            break
    else:
        return True

print(compare(['1','2','4'],['1','4','2']))
print(compare(['1','2'],['3','4']))